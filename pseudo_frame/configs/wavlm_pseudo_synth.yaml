variant_name: "wavlm_pseudo_frame_synth"
backbone: "microsoft/wavlm-base-plus"
backbone_layer: -1

hidden_dim: 256
dropout: 0.2

crop_sec: 10.0
batch_size: 8
num_workers: 2
lr: 1.0e-3
weight_decay: 1.0e-4
epochs: 25
patience: 5
seed: 42

confidence_weight: true
pos_weight: 3.0

split_dir: "whisper-modeling/seen_child_splits_synth_aug"
device: "cuda"
